using AutoMapper;
using MH.Domain.DBModel;

namespace MH.Domain.Mapping
{
    public class LocationMapping : Profile
    {
        public LocationMapping()
        {
            // Basic entity mappings - ViewModels will be created later
            // CreateMap<Province, ProvinceViewModel>();
            // CreateMap<District, DistrictViewModel>();
            // CreateMap<Hospital, HospitalViewModel>();
            // CreateMap<Clinic, ClinicViewModel>();
        }
    }
}